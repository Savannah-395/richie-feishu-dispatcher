{
  "name": "richie-feishu-dispatcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Richie Feishu long-connection bot with Codex CLI delegation and Git-synced local skills.",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/config.js && node --check src/index.js && node --check src/message-card.js && node --check src/mention-utils.js && node --check src/codex-runner.js && node --check src/durable-state.js && node --check src/task-intake.js && node --check src/skill-sync.js && node --check src/thread-queue.js && node --check src/sync-once.js && node --test",
    "test": "node --test",
    "sync:once": "node src/sync-once.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.73.3",
    "@openai/codex": "^0.153.4",
    "dotenv": "^17.4.2",
    "openai": "^7.10.0"
  }
}
